apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
    role: key-value-store
    tier: backend
spec:
  ports:
    # the port that this service should serve on
  - port: 6379
    targetPort: 6379
  selector:
    app: redis
    role: key-value-store
    tier: backend